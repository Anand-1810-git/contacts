PORT=5000
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/smart_contacts
JWT_SECRET=replace_with_a_secure_secret
CLIENT_URL=http://localhost:3000
# Optional, only required if using backend cloud vision OCR endpoint
# GOOGLE_APPLICATION_CREDENTIALS=./service-account.json
